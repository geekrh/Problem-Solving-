#include <iostream>
#define repeat(nb) for (int _loop = 1, _max = (nb); _loop <= _max; _loop++)

#include "robot.h"
using namespace std;
int main()
{
   repeat(21){
   haut();
   droite();
  }
    repeat (21) {
   gauche();
   bas();}
   



}
