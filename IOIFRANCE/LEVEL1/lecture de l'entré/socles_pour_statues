#include <iostream>
#define repeat(nb) for (int _loop = 1, _max = (nb) ; _loop <= _max ; _loop++)
using namespace std;

int main()
{
    int a,b,c;
    cin>>a>>b;

    int j=0;
    for (int i=b;i<=a;i++){
       j+=i*i;
    }
    cout<<j;
}
