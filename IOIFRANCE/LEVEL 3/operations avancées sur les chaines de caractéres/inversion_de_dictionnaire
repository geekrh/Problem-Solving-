#include <bits/stdc++.h>

using namespace std;

int main()
{
  vector <string> v ;

  int n ;
  cin>>n ;

  for (int i=0;i<n;i++) {
    string a,b;
    cin>>a>>b;
    v.push_back(b+' '+a);
  }
  sort(v.begin(),v.end());
  for(int i=0;i<v.size();i++){
    cout<<v[i]<<endl ;
  }
}
